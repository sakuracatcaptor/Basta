const now = new Date();
const distance = Date() ;
console.log(distance)

// data: () = {
//     const pomodoroDuration = 25;    
//     return {
//         currenTimeInSeconds: pomodoroDuration = * 60,
//     },
// },
// mounted: () = {
//     duration: 25 * 60 * 1000,
// },
// computed: {
//     timeDisplay(){
//         const minutes = pareseInt(this.currenTimeInSeconds / 60);
//         const seconds = this.currenTimeInSeconds % 60;
//         const paddedMinut = ("0" + minutes).slice(-2);
//         const paddedSeconds = ("0" + minutes).slice(-2);
//         return '${paddedMinutes}:${paddedSeconds}';
//     }
// }